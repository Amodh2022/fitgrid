include: ../analysis_options.yaml

linter:
  rules:
    # A benchmark's whole output is what it prints. Routing that through a
    # logging framework would hide it behind a log level.
    avoid_print: false
